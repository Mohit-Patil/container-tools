# =============================================================================
# AI CLI Tools Container - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values.
# These values will be available inside the container.
#
# Usage: cp .env.example .env && vim .env
# =============================================================================

# --- API Keys (for tools that support them) ---
# Anthropic API key for Claude-based tools (Aider, etc.)
# ANTHROPIC_API_KEY=sk-ant-...

# OpenAI API key for GPT-based tools (Codex, Aider, etc.)
# OPENAI_API_KEY=sk-...

# Google API key for Gemini
# GOOGLE_API_KEY=...

# --- Aider Configuration ---
# Default model for Aider
# AIDER_MODEL=sonnet

# Disable auto-commits in Aider
# AIDER_AUTO_COMMITS=false

# --- Git Configuration ---
# Use these if you don't want to mount .gitconfig from host
# GIT_AUTHOR_NAME=Your Name
# GIT_AUTHOR_EMAIL=your@email.com
# GIT_COMMITTER_NAME=Your Name
# GIT_COMMITTER_EMAIL=your@email.com

# --- GitHub Token ---
# For non-interactive gh CLI authentication
# GH_TOKEN=ghp_...

# --- Claude Code Agent Teams ---
# Agent teams are enabled by default in this container.
# Set to 0 to disable if not needed.
# CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=0

# Teammate display mode: "auto", "in-process", or "tmux"
# "tmux" gives each teammate its own split pane (tmux is pre-installed)
# "in-process" keeps all teammates in the main terminal
# CLAUDE_TEAMMATE_MODE=auto

# --- Proxy Configuration ---
# If you're behind a corporate proxy
# HTTP_PROXY=http://proxy:8080
# HTTPS_PROXY=http://proxy:8080
# NO_PROXY=localhost,127.0.0.1

# --- AWS Configuration ---
# For Amazon Q (if not using interactive login)
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...
